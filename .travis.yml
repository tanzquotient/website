notifications:
  email:
    - renato-jun.bellotti@student.ethz.ch

sudo: required

services:
  - docker

before_install:
  - source configurations/.env_test

install:
  - docker-compose -f docker-compose.yml -f docker-compose.test.yml build
