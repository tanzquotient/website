# Load testing

Load testing using https://locust.io/.

Currently, this is run manually on-demand from a developer's laptop.

Steps:

1. `locust -f loadtesting/locustfile.py`
2. Go to http://localhost:8089/
3. Start the test and see the results
